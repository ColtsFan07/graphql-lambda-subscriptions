import { ServerClosure } from '../types';
export declare const buildContext: ({ server, connectionInitPayload, connectionId }: {
    connectionInitPayload: object;
    server: ServerClosure;
    connectionId: string;
}) => any;

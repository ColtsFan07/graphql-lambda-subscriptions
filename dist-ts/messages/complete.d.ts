import { CompleteMessage } from 'graphql-ws';
import { MessageHandler } from '../types';
/** Handler function for 'complete' message. */
export declare const complete: MessageHandler<CompleteMessage>;
